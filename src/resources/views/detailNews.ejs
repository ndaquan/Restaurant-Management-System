<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Chi tiết bài viết</title>
  <link rel="stylesheet" href="/css/detailNews.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
</head>
<body>

<header class="news-header">
  <button class="btn-return fas fa-chevron-left fa-lg" onclick="window.location.href='/owner/adminNews'"></button>
  <h1>Chi tiết bài viết</h1>
</header>

<div class="detail-container">
  <div class="image-container">
    <img src="<%= article.image %>" alt="<%= article.title %>">
  </div>

  <h2 class="news-title"><%= article.title %></h2>

  <div class="info-row description-row">
    <span class="info-label">Nội dung:</span>
    <span class="info-content clamped"><%= article.content %></span>
  </div>
  
  <div class="info-row">
    <span class="info-label">Ngày đăng:</span>
    <span class="info-content"><%= new Date(article.createdAt).toLocaleDateString('vi-VN') %></span>
  </div>
  
</div>

</body>
</html>
